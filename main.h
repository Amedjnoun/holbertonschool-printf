#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>

/* Prototype de la fonction _printf */
int _printf(const char *format, ...);
int print_integer_recursive(int n);

#endif /* MAIN_H */
